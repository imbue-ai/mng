{"issue_type": "incorrect_annotation", "file": "libs/mng/imbue/mng/hosts/host.py", "line": 97, "description": "_check_local_symlink_state is decorated with @pure but performs filesystem I/O (is_symlink(), resolve(), exists() on Path objects). The @pure decorator documents that a function 'does not perform I/O operations' and 'returns the same output for the same inputs', neither of which is true for this function.", "confidence": 0.95, "severity": "MINOR"}
