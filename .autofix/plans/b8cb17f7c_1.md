# Issue 1: Unused caplog parameter in test

## What and where
File: `libs/mng/imbue/mng/hosts/host_test.py`, line 2224
Function: `test_apply_work_dir_extra_paths_share_same_host_source_missing_warns`

The `caplog` fixture parameter is declared but never used or asserted against.

## Why it is a problem
The unused parameter suggests the test intended to verify the warning message but doesn't actually do so. Since loguru doesn't propagate to standard Python logging, `caplog` wouldn't capture loguru warnings anyway. The parameter is dead code.

## Planned fix
Remove the `caplog: pytest.LogCaptureFixture` parameter from the function signature.

## Risks
None -- removing an unused parameter has no functional impact.
