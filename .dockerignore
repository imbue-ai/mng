# Exclude .git/ from Docker builds entirely.
# Git state is delivered via a tarball (current.tar.gz), not the live .git dir.
# Files under .git/ (refs, worktrees, FETCH_HEAD, etc.) are actively modified
# by running agents, which races with Modal's upload and causes
# "modified during build process" errors.
.git/
