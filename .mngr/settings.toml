is_allowed_in_pytest = false

[pre_command_scripts]
create = ["""bash -c 'HASH=e66b24ce9354eb11c9dc107b3d640a790f0c45b1; mkdir -p ~/.mngr/dev/ && [ -d "$HOME/.mngr/dev/$HASH" ] || (set -eo pipefail; tmp=$(mktemp -d); rm -rf "$tmp"; git clone . "$tmp" && git -C "$tmp" checkout "$HASH" && mv "$tmp" "$HOME/.mngr/dev/$HASH"); ln -fs "$HOME/.mngr/dev/$HASH" "$HOME/.mngr/dev/current"'"""]

[commands.create]
add_command = ["reviewer_0=claude --dangerously-skip-permissions"]
