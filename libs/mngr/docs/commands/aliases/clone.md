# mngr clone [future] - CLI Options Reference

Create a new agent by cloning an existing one.

Basically just an alias for `mngr create --from-agent <agent>`

Note: The `--snapshot-source` flag [future] requires implementing `_snapshot_source_agent()` for proper remote agent cloning.
